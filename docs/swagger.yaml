basePath: /api
definitions:
  models.User:
    type: object
host: localhost:8080
info:
  contact:
    email: hassanisavailable@gmail.com
    name: Hassan
  description: This is a sample Go API server using Fiber and GORM.
  title: Go API Server
  version: "1.0"
paths:
  /users:
    post:
      consumes:
      - application/json
      description: Creates a user with email and password
      parameters:
      - description: User info
        in: body
        name: user
        required: true
        schema:
          $ref: '#/definitions/models.User'
      produces:
      - application/json
      responses:
        "201":
          description: Created
          schema:
            $ref: '#/definitions/models.User'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Create a new user
      tags:
      - Users
swagger: "2.0"
